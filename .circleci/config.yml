version: 2.1
orbs:
  node: circleci/node:5.1.0
jobs:
  build:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - checkout
      - node/install:
          node-version: '16.19.0'
      - run: npm ci
      - run: npx hardhat test
